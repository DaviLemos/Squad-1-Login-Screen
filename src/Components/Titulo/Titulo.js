import styled from 'styled-components';

export const Titulo = styled.p`
  width: 81px;
  height: 38px;

  margin: 0 0 32px 0;

  font-family: Mark Pro;
  font-style: normal;
  font-weight: normal;
  font-size: 30px;
  line-height: 38px;

  color: #e0e0e0;
`;
